<h1>Listing students</h1>

<%= link_to 'Subjects', subjects_path %> | 
<%= link_to 'Assignments', assignments_path %> |
<%= link_to 'Resources', resources_path %> 
<br/>
<%= link_to 'Create Assignment List', students_select_daily_path %> 

  <table>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Parent</th>
      <th>Phone h</th>
      <th>Phone c</th>
      <th>Phone w</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  
  <% @students.each do |student| %>
    <tr>
      <td><%= student.name %></td>
      <td><%= student.email %></td>
      <td><%= student.parent %></td>
      <td><%= student.phone_h %></td>
      <td><%= student.phone_c %></td>
      <td><%= student.phone_w %></td>
      <td><%= link_to 'Show', student %></td>
      <td><%= link_to 'Edit', edit_student_path(student) %></td>
      <td><%= link_to 'Select Subjects', select_subjects_student_path(student) %></td>
    <% end %>
    </tr>
  </table>
<br/>

<%= link_to 'New Student', new_student_path %>
